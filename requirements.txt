# MarathiTTSv1 — Marathi TTS Dependencies
# Install with: pip install -r requirements.txt
# Then install PyTorch separately: pip install torch torchaudio

# Data processing
librosa>=0.10.0
soundfile>=0.12.0
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
tqdm>=4.64.0

# Training (Piper uses PyTorch Lightning)
pytorch-lightning>=1.7.0,<2.0.0
torchmetrics>=0.11.0,<1.0.0

# NOTE: torch and torchaudio are NOT listed here because the correct version
# depends on your CUDA version. Install separately:
#   CPU only:    pip install torch torchaudio
#   CUDA 11.8:   pip install torch torchaudio --extra-index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1:   pip install torch torchaudio --extra-index-url https://download.pytorch.org/whl/cu121

# NOTE: num2words is NOT included — it does not support Marathi.
# The normalizer uses a built-in lookup table instead.
